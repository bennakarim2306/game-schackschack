import { Text, View } from "react-native";

const GameSettings = () => {
    return (
        <View>
            <Text>
                here is the new view
            </Text>
        </View>
    );
}

export default GameSettings;
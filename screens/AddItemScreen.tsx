import React from "react";
import { View, Text } from "react-native";

const AddItemScreen = () => {
    return (
        <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
            <Text>Add an item to sell</Text>
        </View>
    );
};

export default AddItemScreen;